-- Database charset configuration
ALTER DATABASE liftupai_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Create tables if needed (Spring Boot will handle this with ddl-auto: update)
-- Add any initial data here if required

-- Example: Create indexes for better performance
-- CREATE INDEX idx_user_email ON users(email);
-- CREATE INDEX idx_workout_user ON workout_sessions(user_id);